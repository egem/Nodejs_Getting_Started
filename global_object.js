require('./my_packages/global_object');

// setTimeout belogns to global object.
// We can also call global object as follows
// global.setTimeout
// Since it is inside global object, we can omit global keyword as we do for answer variable.
console.log(answer);
